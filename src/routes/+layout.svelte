<!--
  BendScript is a fullscreen interactive app (scroll-snap homepage +
  fullscreen graph canvas + authenticated routes). A fixed portfolio
  nav conflicts with every screen's 100vh design, so <amp-nav> is
  intentionally not rendered here. The amp-nav.js component still
  ships in static/ so dedicated marketing pages can opt in later.
-->
<slot />
